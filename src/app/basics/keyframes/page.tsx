import PreviewSections from "@/components/preview-sections";

import sectionsKeyframes from "./data/sections-keyframes";

const Keyframes = () => {
  return <PreviewSections sections={sectionsKeyframes} />;
};

export default Keyframes;
