import PreviewSections from "@/components/preview-sections";

import sectionsVariants from "./data/sections-variants";

const Variants = () => {
  return <PreviewSections sections={sectionsVariants} />;
};

export default Variants;
