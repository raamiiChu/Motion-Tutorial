import PreviewSections from "@/components/preview-sections";

import sectionWhileProps from "./data/section-while-props";

const WhileProps = () => {
  return <PreviewSections sections={sectionWhileProps} />;
};

export default WhileProps;
