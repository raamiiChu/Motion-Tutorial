import PreviewSections from "@/components/preview-sections";

import sectionDrag from "./data/section-drag";

const Drag = () => {
  return <PreviewSections sections={sectionDrag} />;
};

export default Drag;
